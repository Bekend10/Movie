# Production Environment Variables

# API Base URL (OPhim API - public, không cần auth)
VITE_API_URL=https://ophim1.com

# Auth API (KHÔNG CẦN - app đang dùng mock auth + localStorage)
# Chỉ cần khi tích hợp backend thật trong tương lai
# VITE_AUTH_API_URL=https://your-backend-api.com/api

# App Config
VITE_APP_NAME=MovieApp
VITE_APP_VERSION=1.0.0

# App Environment
VITE_APP_ENV=production
